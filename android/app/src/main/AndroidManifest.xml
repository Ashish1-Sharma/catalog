<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-sdk tools:overrideLibrary="com.google.firebase, com.google.firebase.measurement, com.google.firebase.measurement_impl, com.google.firebase.common, com.google.firebase.components, com.google.firebase.concurrent, com.google.firebase.installations, com.google.firebase.installations.interop, com.google.firebase.firebase_analytics, com.google.firebase.analytics, com.google.firebase.analytics.connector, com.google.android.gms, com.google.android.gms.common, com.google.android.gms.base, com.google.android.gms.measurement, com.google.android.gms.measurement_impl, com.google.android.gms.measurement_base, com.google.android.gms.measurement_sdk, com.google.android.gms.measurement_sdk_api, com.google.android.gms.measurement.api, com.google.android.gms.measurement.base, com.google.android.gms.measurement.sdk, com.google.android.gms.measurement.sdk.api, com.google.android.gms.measurement.impl, com.google.android.gms.tasks, com.google.firebase.datatransport, io.flutter.plugins.firebase.analytics, io.flutter.plugins.firebase.core" />

    <!-- Saving the catalog PDF into the public Downloads folder.
         Android 10+ uses scoped storage and does not need this. -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />

    <application
        android:label="Catalog Maker"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- UCropActivity required by image_cropper plugin -->
        <activity
            android:name="com.yalantis.ucrop.UCropActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"/>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
